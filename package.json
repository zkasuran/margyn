{
  "name": "margyn",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "description": "Proves your checks do not check anything. Every finding ships a reproduction.",
  "license": "MIT",
  "engines": {
    "node": ">=22"
  },
  "bin": {
    "margyn": "./src/cli.mjs"
  },
  "scripts": {
    "test": "node --test test/*.test.mjs",
    "scan": "node src/cli.mjs",
    "dev": "node web/server.mjs",
    "worker:dev": "wrangler dev",
    "worker:deploy": "wrangler deploy"
  },
  "devDependencies": {
    "wrangler": "^4.118.0"
  }
}
